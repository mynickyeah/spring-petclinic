version: "3"

services:
  db:
    build:
      context: ${REPO_URL}#${BUILD_BRANCH}
      dockerfile: ${DB_DF_PATH}
    image: mysql:${DB_TAG}
  app:
    build:
      context: ${REPO_URL}#${BUILD_BRANCH}
      dockerfile: ${APP_DF_PATH}
    image: spring:${APP_TAG}
    depends_on:
      - db
    environment:
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}


